# BMAI Environment Variables
# Copy this file to .env and fill in your actual values
# IMPORTANT: .env is gitignored - never commit it!

# === Supabase Configuration (Required) ===
# Get these from: Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key

# === Service Role Key (Server-side only) ===
# WARNING: Never expose this in client code
# Used for admin operations and PDF ingestion pipeline
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key

# === Database Configuration ===
# Get this from: Supabase Dashboard → Settings → Database
SUPABASE_DB_PASSWORD=your-database-password

# === AI / Embeddings Provider ===
# Required for semantic search functionality
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key

# Optional: If using Anthropic Claude for chat
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# === Application Settings ===
# Environment: development, staging, production
NODE_ENV=development

# === Optional: Cloud Storage ===
# If storing files in cloud storage instead of Supabase Storage
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=ap-southeast-2

# === Optional: Email Configuration ===
# For service request notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password
# SERVICE_DESK_EMAIL=servicedesk@mps.com.au
